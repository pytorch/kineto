// This proto describes the diagnostics for debugging profiling issues of
// the TensorBoard profiler.
syntax = "proto3";

package tensorboard.profiler;

message Diagnostics {
  repeated string info = 1;
  repeated string warnings = 2;
  repeated string errors = 3;
}
